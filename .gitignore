# Go
/chanarr
/chanarr-proto
*.test

# Release builds (deploy/build-release.sh)
/dist/

# SQLite
*.db
*.db-journal

# Node / Vite
web/node_modules/
web/dist/

# Frontend build output embedded into the Go binary (internal/webui) —
# generated by `cd web && npm run build`; required before `go build`.
internal/webui/dist/

# Misc
.DS_Store
